<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= check_box_tag(:completed) %>
      <%= label_tag(:completed, task.title) %>
      <%= link_to "Allez voir", task_path(task) %>
    </li>
  <% end %>
</ul>
